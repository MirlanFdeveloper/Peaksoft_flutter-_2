import 'package:flutter/material.dart';

class UsonPage extends StatelessWidget {
  const UsonPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Uson'),),
    );
  }
}